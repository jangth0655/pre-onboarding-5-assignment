import styled from 'styled-components';

const TitleContainer = styled.div`
  width: 100%;
  text-align: center;
  margin-bottom: 50px;
`;
const Title = styled.h1`
  font-weight: bold;
  font-size: 2.5rem;
`;

const S = {
  TitleContainer,
  Title,
};

export default S;
