import styled from 'styled-components';

const BoardSubContainer = styled.div`
  margin-bottom: 1rem;
`;
const BoardSubText = styled.h1`
  color: #a2a5aa;
  font-weight: bold;
`;

const S = {
  BoardSubContainer,
  BoardSubText,
};

export default S;
